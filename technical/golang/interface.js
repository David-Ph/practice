/* 
interface adalah tipe data abstract, dia tidak memiliki implemetnasi langsung
sebuah interface berisikan definisi method
biasanya interface digunakan sebagai kontrak

setiap data/interface yang sesuai dengan kontrak interface, secara otomatis dianggap sebagai interface tersebut

interface kosong adalah interface yang tidak memiliki deklarasi method apapun. Hal ini membuat secara otomatis semua tipe data adalah implementasinya.
*/